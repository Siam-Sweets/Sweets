# PosApp v1.10.37 Fix Note

## Fixed

- Added visible calendar selection buttons to the Sales History From and To date fields.
- Replaced the fixed-width Sales History filter grid with a wrapping filter bar.
- Kept the existing filter, export, print, and sales-loading behavior unchanged.

## Validation

- Parsed the edited XAML and project XML successfully.
- Could not run a .NET build locally because the `dotnet` SDK is not installed in this environment.
