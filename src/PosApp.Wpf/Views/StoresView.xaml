<UserControl x:Class="PosApp.Wpf.Views.StoresView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="{StaticResource PagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{DynamicResource Store_Title}" Style="{StaticResource Heading1}"/>
            <TextBlock Text="{DynamicResource Store_Subtitle}" Style="{StaticResource Muted}"/>
        </StackPanel>

        <WrapPanel Grid.Row="1" Margin="0,0,0,12">
            <Button Content="{DynamicResource Store_Add}" Style="{StaticResource PrimaryButton}" Click="Add_Click" Margin="0,0,8,0"/>
            <Button Content="{DynamicResource Common_Edit}" Style="{StaticResource OutlineButton}" Click="Edit_Click" Margin="0,0,8,0"/>
            <Button Content="{DynamicResource Store_Switch}" Style="{StaticResource SuccessButton}" Click="Switch_Click" Margin="0,0,8,0"/>
            <Button Content="{DynamicResource Store_ToggleActive}" Style="{StaticResource OutlineButton}" Click="ToggleActive_Click"/>
        </WrapPanel>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="StoresGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Store_Code}" Binding="{Binding Code}" Width="110"/>
                    <DataGridTextColumn Header="{DynamicResource Store_Name}" Binding="{Binding Name}" Width="220"/>
                    <DataGridTextColumn Header="{DynamicResource Store_Address}" Binding="{Binding Address}" Width="*" MinWidth="220"/>
                    <DataGridTextColumn Header="{DynamicResource Store_Phone}" Binding="{Binding Phone}" Width="150"/>
                    <DataGridCheckBoxColumn Header="{DynamicResource Store_Active}" Binding="{Binding IsActive}" Width="80"/>
                    <DataGridCheckBoxColumn Header="{DynamicResource Store_Current}" Binding="{Binding IsCurrent}" Width="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
