<UserControl x:Class="PosApp.Wpf.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:PosApp.Wpf.Converters"
             Background="{DynamicResource BackgroundBrush}">
    <UserControl.Resources>
        <conv:MoneyConverter x:Key="Money"/>
        <conv:ActiveActionConverter x:Key="ActiveAction"/>
        <conv:ProductSaleModeConverter x:Key="SaleMode"/>
    </UserControl.Resources>
    <Grid Margin="{StaticResource PagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="{DynamicResource Prod_Title}" Style="{StaticResource Heading1}"/>
                <TextBlock Text="{DynamicResource Prod_Subtitle}" Style="{StaticResource Muted}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="{DynamicResource Prod_ImportCsv}" Style="{StaticResource OutlineButton}"
                        Click="ImportCsv_Click" Margin="0,0,8,0" Padding="16,10"/>
                <Button Content="{DynamicResource Prod_ExportCsv}" Style="{StaticResource OutlineButton}"
                        Click="ExportCsv_Click" Margin="0,0,8,0" Padding="16,10"/>
                <Button Content="{DynamicResource Prod_Add}" Style="{StaticResource PrimaryButton}"
                        Click="Add_Click" Padding="20,12"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="SearchBox" TextChanged="Search_TextChanged"
                     Padding="12,10" FontSize="14"/>
            <ComboBox Grid.Column="1" x:Name="CategoryFilter" Margin="12,0,0,0"
                      SelectionChanged="CategoryFilter_SelectionChanged"/>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="ProductsGrid" ItemsSource="{Binding}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Prod_Name}" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource Prod_SkuOnly}" Binding="{Binding Sku}" Width="110"/>
                    <DataGridTextColumn Header="{DynamicResource Prod_Barcode}" Binding="{Binding Barcode}" Width="130"/>
                    <DataGridTextColumn Header="{DynamicResource Prod_Category}" Binding="{Binding Category.Name}" Width="140"/>
                    <DataGridTextColumn Header="{DynamicResource Prod_Price}" Binding="{Binding Price, Converter={StaticResource Money}}" Width="100"/>
                    <DataGridTextColumn Header="{DynamicResource Prod_Cost}" Binding="{Binding CostPrice, Converter={StaticResource Money}}" Width="100"/>
                    <DataGridTextColumn Header="{DynamicResource Prod_Stock}" Binding="{Binding StockQuantity, StringFormat=' {0:0.###}'}" Width="80"/>
                    <DataGridTextColumn Header="{DynamicResource Prod_SaleMode}"
                                        Binding="{Binding SaleMode, Converter={StaticResource SaleMode}}" Width="100"/>
                    <DataGridTextColumn Header="{DynamicResource Prod_Unit}" Binding="{Binding UnitSymbol}" Width="65"/>
                    <DataGridTemplateColumn Header="{DynamicResource Usr_Active}" Width="65">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive, Mode=OneWay}"
                                          Tag="{Binding}"
                                          PreviewMouseLeftButtonDown="Active_PreviewMouseLeftButtonDown"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Focusable="False"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="{DynamicResource Common_Actions}" Width="175">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="{DynamicResource Common_Edit}" Style="{StaticResource OutlineButton}"
                                            Padding="10,4" Margin="2" Click="Edit_Click" Tag="{Binding}"/>
                                    <Button Content="{Binding IsActive, Converter={StaticResource ActiveAction}}"
                                            Style="{StaticResource OutlineButton}" Padding="10,4" Margin="2"
                                            Click="Delete_Click" Tag="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
