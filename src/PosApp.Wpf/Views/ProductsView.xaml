<UserControl x:Class="PosApp.Wpf.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:PosApp.Wpf.Converters"
             Background="{DynamicResource BackgroundBrush}">
    <UserControl.Resources>
        <conv:MoneyConverter x:Key="Money"/>
    </UserControl.Resources>
    <Grid Margin="{StaticResource PagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="{DynamicResource Prod_Title}" Style="{StaticResource Heading1}"/>
                <TextBlock Text="Manage your product catalog, prices, and SKUs" Style="{StaticResource Muted}"/>
            </StackPanel>
            <Button Grid.Column="1" Content="{DynamicResource Prod_Add}" Style="{StaticResource PrimaryButton}"
                    Click="Add_Click" Padding="20,12"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="SearchBox" TextChanged="Search_TextChanged"
                     Padding="12,10" FontSize="14"/>
            <ComboBox Grid.Column="1" x:Name="CategoryFilter" Margin="12,0,0,0"
                      SelectionChanged="CategoryFilter_SelectionChanged"/>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="ProductsGrid" ItemsSource="{Binding}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="120"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="140"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat='৳ {0:0.00}'}" Width="100"/>
                    <DataGridTextColumn Header="Cost" Binding="{Binding CostPrice, StringFormat='৳ {0:0.00}'}" Width="100"/>
                    <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity, StringFormat=' {0:0.###}'}" Width="80"/>
                    <DataGridCheckBoxColumn Header="Weighted" Binding="{Binding IsWeighted}" Width="80"/>
                    <DataGridTemplateColumn Header="Actions" Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit" Style="{StaticResource OutlineButton}"
                                            Padding="10,4" Margin="2" Click="Edit_Click" Tag="{Binding}"/>
                                    <Button Content="Delete" Style="{StaticResource DangerButton}"
                                            Padding="10,4" Margin="2" Click="Delete_Click" Tag="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
