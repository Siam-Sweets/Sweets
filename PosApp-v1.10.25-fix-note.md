# PosApp v1.10.25 Fix Note

## Fixed

- Fixed the Store filter selected value showing `StoreFilterOption { Id = ... }`.
- Dashboard and Reporting store filters now show the selected store name properly.
- The dropdown list and selected field both use the store `Name`.

## Validation

- Checked edited XAML/XML files for well-formed markup.
- Local compilation was not run in this workspace because the .NET SDK is not installed.
