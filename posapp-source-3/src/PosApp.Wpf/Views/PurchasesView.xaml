<UserControl x:Class="PosApp.Wpf.Views.PurchasesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="{StaticResource PagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Purchases &amp; Suppliers" Style="{StaticResource Heading1}"/>
                <TextBlock Text="Receive supplier deliveries, update stock, and keep a local purchase history"
                           Style="{StaticResource Muted}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Add Supplier" Style="{StaticResource OutlineButton}"
                        Click="AddSupplier_Click" Margin="0,0,8,0"/>
                <Button Content="New Purchase" Style="{StaticResource PrimaryButton}"
                        Click="NewPurchase_Click"/>
            </StackPanel>
        </Grid>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,16">
            <Border Style="{StaticResource StatCard}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Purchase Documents" Style="{StaticResource Muted}"/>
                    <TextBlock x:Name="DocumentCountText" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCard}" Margin="8,0">
                <StackPanel>
                    <TextBlock Text="Purchase Total" Style="{StaticResource Muted}"/>
                    <TextBlock x:Name="PurchaseTotalText" FontSize="24" FontWeight="Bold"
                               Foreground="{DynamicResource InfoTextBrush}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCard}" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="Active Suppliers" Style="{StaticResource Muted}"/>
                    <TextBlock x:Name="SupplierCountText" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="From" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <DatePicker x:Name="FromPicker" Width="140"/>
            <TextBlock Text="To" VerticalAlignment="Center" Margin="16,0,6,0"/>
            <DatePicker x:Name="ToPicker" Width="140"/>
            <Button Content="Refresh" Style="{StaticResource OutlineButton}"
                    Click="Refresh_Click" Margin="12,0,0,0" Padding="14,7"/>
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0" Margin="0,0,8,0">
                <DockPanel>
                    <TextBlock Text="Posted Purchases" DockPanel.Dock="Top" FontWeight="SemiBold"
                               Margin="16,16,16,8"/>
                    <DataGrid x:Name="PurchasesGrid" AutoGenerateColumns="False" IsReadOnly="True"
                              MouseDoubleClick="PurchasesGrid_MouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Document" Binding="{Binding DocumentNumber}" Width="145"/>
                            <DataGridTextColumn Header="Date" Binding="{Binding DocumentDateLocal, StringFormat='{}{0:dd MMM yyyy HH:mm}'}" Width="145"/>
                            <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier.Name}" Width="*"/>
                            <DataGridTextColumn Header="Reference" Binding="{Binding ExternalReference}" Width="120"/>
                            <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat='৳ {0:0.00}'}" Width="105"/>
                            <DataGridTemplateColumn Header="" Width="70">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="View" Style="{StaticResource OutlineButton}"
                                                Padding="8,4" Margin="2" Click="ViewPurchase_Click" Tag="{Binding}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource Card}" Padding="0" Margin="8,0,0,0">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="16,16,16,8">
                        <TextBlock Text="Suppliers" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <TextBox x:Name="SupplierSearchBox" TextChanged="SupplierSearch_TextChanged"
                                 Padding="10,7" ToolTip="Search supplier"/>
                    </StackPanel>
                    <DataGrid x:Name="SuppliersGrid" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="110"/>
                            <DataGridTemplateColumn Header="" Width="65">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Edit" Style="{StaticResource OutlineButton}"
                                                Padding="7,4" Margin="2" Click="EditSupplier_Click" Tag="{Binding}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
